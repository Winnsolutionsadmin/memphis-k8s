---
name: active context — memphis-k8s
description: Where the last session left off. Read at session start, update at session end.
type: infra
---

# Active Context

> **Read this first.** Update at the END of every session via `/wrap-session` — this is the resume mechanism between sessions.

## Last session

- **Date:** 2026-06-02 (retrofitted via the canonical scaffold — no working session yet on the pattern)
- **What was done:** Retrofitted 2026-06-02 to the canonical scaffold; no working session yet on the pattern; existing state preserved. Added CLAUDE.md (cold-start ritual + accurate description) and memory-bank/. No chart files touched.

## Where I am now

This is a mirror of the official `memphisdev/memphis-k8s` Helm chart repo under Jarred's `Winnsolutionsadmin` org. Git history is upstream's: first commit Feb 2022, latest `eecf250 Update values.yaml` (Apr 2023). Chart version `0.4.5`. From Jarred's side it is a reference/mirror with no in-house changes — looks-archived. The deployable substance is the `memphis/` Helm chart; packaged releases live in `charts/`, served as a Helm repo via GitHub Pages (CNAME `k8s.memphis.dev`).

## Immediate next step

(Define on next working session. If there's no active reason to modify this mirror, leave it as a read-only reference.)

## Open questions / blockers

- Why is this mirrored into Winnsolutionsadmin? (Reference for a deployment, evaluation of Memphis as a broker, or just an archived clone?) Capture the answer in projectbrief.md when known.

## Open loops

<!-- Things in flight that aren't done but need to be remembered. -->
- (none flagged at retrofit time)
